import { Typography } from "@mui/material";

const Auth = () => {
    return (
        <div>
            <Typography variant="h5" color="initial">
                Auth component
            </Typography>
        </div>
    );
};

export default Auth;
